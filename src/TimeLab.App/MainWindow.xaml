<Window x:Class="TimeLab.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TimeLab.App"
        xmlns:vm="clr-namespace:TimeLab.App.ViewModels"
        Title="TimeLab" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="400" />
        </Grid.ColumnDefinitions>

        <!-- 左侧：Todo 列表 -->
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="任务" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" />

            <DockPanel Grid.Row="1" Margin="0,0,0,10">
                <Button DockPanel.Dock="Right" Content="添加" Width="50"
                        Command="{Binding AddTaskCommand}" Margin="5,0,0,0" />
                <TextBox Text="{Binding NewTaskTitle, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>

            <ListBox Grid.Row="2" ItemsSource="{Binding Tasks}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel Margin="0,2">
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <Button Content="完成" Width="40"
                                        Command="{Binding DataContext.CompleteTaskCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}" Margin="2,0" />
                                <Button Content="删除" Width="40"
                                        Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}" />
                            </StackPanel>
                            <CheckBox IsChecked="{Binding IsCompleted}" IsHitTestVisible="False" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Title}" VerticalAlignment="Center" Margin="5,0,10,0" />
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <!-- 右侧 -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 番茄钟 -->
            <GroupBox Grid.Row="0" Header="番茄钟" Margin="0,0,0,10">
                <StackPanel Margin="10">
                    <TextBlock Text="{Binding StatusText, StringFormat='状态: {0}'}" FontSize="14" />
                    <TextBlock Text="{Binding ElapsedTimeDisplay}" FontSize="36"
                               HorizontalAlignment="Center" Margin="0,10" FontFamily="Consolas" />

                    <ComboBox ItemsSource="{Binding Tasks}"
                              SelectedItem="{Binding SelectedTask}"
                              DisplayMemberPath="Title"
                              IsEditable="False"
                              Margin="0,0,0,8" />

                    <TextBlock Text="{Binding LinkedTaskTitle, StringFormat='关联: {0}'}"
                               FontSize="12" Foreground="Gray" Margin="0,0,0,5" />

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="开始" Width="70" Margin="3" Command="{Binding StartCommand}" />
                        <Button Content="暂停" Width="70" Margin="3" Command="{Binding PauseCommand}" />
                        <Button Content="停止" Width="70" Margin="3" Command="{Binding StopCommand}" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- Session Log -->
            <GroupBox Grid.Row="1" Header="专注记录">
                <ListBox ItemsSource="{Binding Sessions}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Margin="0,2" FontFamily="Consolas">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0:hh\:mm\:ss} — {1:HH:mm} ~ {2:HH:mm}">
                                        <Binding Path="Duration" />
                                        <Binding Path="StartTime" />
                                        <Binding Path="EndTime" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
